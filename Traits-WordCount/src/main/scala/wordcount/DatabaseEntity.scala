package wordcount

/**
 * Let's pretend this is a base class from a third party persistence
 * framework that all persistent entities need to inherit from.
 */
class DatabaseEntity {
  // A bunch of (imaginary) query/update methods omitted for simplicity...
}